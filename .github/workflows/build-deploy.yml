# DÉSACTIVÉ — 2026-04-03
#
# Ce workflow était redondant : Railway déploie déjà automatiquement
# via son intégration GitHub native (Railway GitHub App) à chaque push
# sur main. Le RAILWAY_TOKEN était expiré, générant ~28 emails d'échec
# par jour pour rien.
#
# Si tu veux réactiver un workflow CI, ajoute d'abord un vrai RAILWAY_TOKEN
# valide dans GitHub → Settings → Secrets → Actions.
#
# Pour l'instant : Railway natif suffit. Pas besoin de ce fichier.

name: Build & Deploy to Railway (désactivé)

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: "Taper 'deploy' pour déclencher manuellement"
        required: true

jobs:
  deploy:
    if: github.event.inputs.confirm == 'deploy'
    runs-on: ubuntu-latest
    steps:
      - name: Info
        run: echo "Déploiement Manuel — Railway déploie automatiquement via GitHub App"
